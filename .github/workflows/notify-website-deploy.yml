name: notify-website-deploy

on:
  push:
    branches:
      - main
      - master
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trigger-website:
    runs-on: ubuntu-latest
    steps:
      - name: Check dispatch token secret
        id: token_check
        env:
          WEBSITE_DISPATCH_TOKEN: ${{ secrets.WEBSITE_DISPATCH_TOKEN || secrets.BLUECOLLAR_WEBSITE_DISPATCH_TOKEN || secrets.REPO_DISPATCH_TOKEN }}
        run: |
          if [ -n "$WEBSITE_DISPATCH_TOKEN" ]; then
            echo "has_token=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_token=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Skip notice (no token configured)
        if: steps.token_check.outputs.has_token != 'true'
        run: |
          echo "::warning::Skipping website dispatch because no valid dispatch token secret is configured."
          echo "Set WEBSITE_DISPATCH_TOKEN (or BLUECOLLAR_WEBSITE_DISPATCH_TOKEN / REPO_DISPATCH_TOKEN) to enable cross-repo dispatch."

      - name: Trigger BlueCollar-Website deployment
        if: steps.token_check.outputs.has_token == 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.WEBSITE_DISPATCH_TOKEN || secrets.BLUECOLLAR_WEBSITE_DISPATCH_TOKEN || secrets.REPO_DISPATCH_TOKEN }}
          script: |
            const isRelease = context.eventName === 'release';
            const eventType = isRelease ? 'product-release' : 'product-update';
            const payload = {
              source_repo: context.repo.owner + '/' + context.repo.repo,
              source_event: context.eventName,
              source_ref: context.ref,
              source_sha: context.sha,
              release_tag: context.payload?.release?.tag_name ?? null,
              release_name: context.payload?.release?.name ?? null,
              release_url: context.payload?.release?.html_url ?? null,
            };

            await github.request('POST /repos/{owner}/{repo}/dispatches', {
              owner: 'BlueCollar-Systems',
              repo: 'BlueCollar-Website',
              event_type: eventType,
              client_payload: payload,
            });

            core.info(`Dispatched ${eventType} to BlueCollar-Website`);

